const chai = require("chai");
const chaiHttp = require("chai-http");
const api = require("../app");

chai.should();
chai.use(chaiHttp);


describe("pruebas back", ()=>{

    var postW;
    describe (" post  notes", (done)=>{
        var try 
    })

})